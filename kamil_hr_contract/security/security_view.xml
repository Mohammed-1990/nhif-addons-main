<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="removing_differences_comp_rule" model="ir.rule">
            <field name="name">Removing differences multi company rule</field>
            <field name="model_id" ref="model_removing_differences"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|',('employee_id.company_id','=',False),('employee_id.company_id','child_of',[user.company_id.id])]</field>
        </record>

        <!-- <record id="removing_differences_internal_auditor_rule" model="ir.rule">
            <field name="name">removing differences internal auditor rule</field>
            <field name="model_id" ref="model_removing_differences"/>
            <field name="domain_force">[('employee_id.company_id','child_of',[user.company_id.id]),('state','in',['internal_auditor','accounts'])]</field>
            <field name="groups" eval="[(4,ref('kamil_hr_security.hr_internal_auditor_group'))]"/>
        </record> -->

    </data>
</odoo>
