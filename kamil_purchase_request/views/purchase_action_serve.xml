<odoo>
    <!-- Server Action: يتحقق من قيمة is_need_request -->
    <record id="action_product_filter_check" model="ir.actions.server">
        <field name="name">Check Product Add</field>
        <field name="model_id" ref="model_purchase_request"/>
        <field name="binding_model_id" ref="model_purchase_request"/>
        <field name="state">code</field>
        <field name="code">
if record.is_need_request:
    raise ValidationError("❌ لا يمكنك إضافة منتجات لأن الحقل 'Need Request' مفعل.")
else:
    action = env.ref('kamil_purchase_request.action_product_filter').read()[0]
        </field>
    </record>



</odoo>
